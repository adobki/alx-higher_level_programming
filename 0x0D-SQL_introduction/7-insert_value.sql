-- Inserts a new row in the table `first_table` of a database in MySQL server.
-- The database name will be passed as an argument of mysql command.
INSERT INTO first_table(id, name) VALUES(89, 'Best School');
