-- Removes all records with a score <= 5 in the table `second_table` of
-- a database in MySQL server.
-- The database name will be passed as an argument of mysql command.
DELETE FROM second_table WHERE score<=5;
