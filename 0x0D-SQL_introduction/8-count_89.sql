-- Displays number of records with id = 89 in the table `first_table` of a
-- database in MySQL server.
-- The database name will be passed as an argument of mysql command.
SELECT COUNT(id) FROM first_table WHERE id = 89;
