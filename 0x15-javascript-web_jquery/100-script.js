// Changes the font color of the header to red after browser
// has loaded the webpage.

window.addEventListener('DOMContentLoaded', (event) => {
  document.querySelector('header').style.color = '#FF0000';
});
