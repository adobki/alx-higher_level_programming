#include <stdlib.h>
#include <stdio.h>

/**
 * main - Function description.
 *
 * @argc: Number of arguments passed to program from the commandline (CL)
 * @argv: Vector of all arguments passed to prongram from CL.
 *
 * Return: EXIT_SUCCESS (0) on success.
 *         EXIT_FAILURE (1) on error.
 */
int main(void)
{
	printf("\n");

	return (EXIT_SUCCESS);
}

/**
 * print_python_list - Function description
 *
 * @p: Number of arguments passed to program from the commandline (CL)
 */
void *print_python_list(void *p)
{
	p = NULL;
	return (p);
}

/**
 * print_python_list - Function description
 *
 * @p: Number of arguments passed to program from the commandline (CL)
 */
void *print_python_bytes(void *p)
{
	p = NULL;
	return (p);
}

/**
 * print_python_list - Function description
 *
 * @p: Number of arguments passed to program from the commandline (CL)
 */
void *print_python_float(void *p)
{
	p = NULL;
	return (p);
}
